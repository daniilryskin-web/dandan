# 07. Справочник датасета: что означает каждый лист и каждый столбец

Описание того, что выдаёт `prepare_dataset.py`. Держите под рукой при сборке чартов — здесь написано, откуда берётся каждое число.

---

## Главное правило расчётов

**Зарплата сравнивается только с проектной ролью.** Проектная роль — это то, что человек фактически делает в проекте, и подогнать её под желаемый оклад трудно.

**Штатная должность в расчётах не участвует вообще.** Её могут назначить формально, чтобы обосновать зарплату: человека берут «руководителем проектов», а работает он администратором. Сравнение с медианой должности в такой ситуации проверяет решение против его же обоснования, и человек оказывается «в норме» по определению.

Должность и организация остались в датасете **справочными колонками**: их видно в реестре, по ним можно фильтровать и группировать, но ни на одну расчётную цифру они не влияют. Если у кого-то Compa-ratio 2.09, а в графе должности стоит «Руководитель проекта» при роли «Администратор проекта» — это видно глазами, но цифра посчиталась без участия должности.

---

## Файлы на выходе

| Файл | Что это | Куда идёт |
|---|---|---|
| `datalens_dataset.csv` | Основная таблица назначений — одна строка на строку исходной таблицы, плюс рассчитанные столбцы | **Это грузим в DataLens** |
| `datalens_dataset_roles.csv` | По одной строке на роль | Датасет для чарта разброса |
| `datalens_dataset_tree.csv` | Пара «продукт — сотрудник», 218 строк | Датасет дерева структуры |
| `datalens_dataset_alignment.csv` | Пара «сценарий — сотрудник», 78 строк | Датасет вкладки выравнивания |
| `datalens_dataset_history.csv` | Все накопленные срезы одним файлом | Датасет вкладки динамики |
| `datalens_dataset_changes.csv` | Что изменилось с прошлого среза | Таблица «Изменения» на дашборде |
| `datalens_dataset.xlsx` | То же самое плюс десять сводных листов | Для чтения глазами и проверки перед загрузкой |
| `history/ГГГГ-ММ-ДД.csv` | Архив срезов | Не грузится; из него собирается история |

---

# Часть 1. Листы Excel-файла

## Лист «Назначения»

Основная таблица: то же, что в CSV. Одна строка = одно назначение человека (или вакансии) на продукт в определённой роли. Это единственный лист, который нужен DataLens; остальные шесть — для проверки и чтения.

**Зернистость важна:** сотрудник, занятый на трёх продуктах, занимает три строки с одинаковой зарплатой. Поэтому суммировать `ЗП` по этому листу нельзя — для денег есть `ФОТ аллоцированный`, для численности `Первичное назначение`. Подробно — в разделе про столбцы.

## Лист «Сотрудники»

Один человек — одна строка (вакансии исключены). Это **рабочий реестр для разбора зарплат**, отсортированный по `Отклонение руб` от самой большой недоплаты к самой большой переплате.

Порядок колонок здесь не случаен: сначала роль и всё, что от неё считается, потом справочные должность с организацией. Читается как «за эту работу платят столько → а этому человеку столько → отсюда отклонение».

**Столбцы `Блок`, `Проект`, `Руководитель` могут содержать несколько значений через `; `.** Человек, занятый в двух проектах, работает в обоих, и раньше он показывался только в том, где стоит его первичное назначение, — молча и без признака, что что-то потеряно. Сейчас таких восемь человек из 180. Значения отсортированы по алфавиту, а не по первичности, чтобы строка не «прыгала» между выгрузками.

Из-за этого группировать лист по `Блок` или `Проект` нельзя: «Зазыкин В.Г.; Радужан А.А.» станет отдельной категорией. Для разрезов по направлениям есть лист «Назначения», где одна строка — одно назначение.

Открывать первым при квартальном разборе.

## Лист «Оценка ЗП»

**Готовый анализ: кому недоплачивают, кому переплачивают, у кого зарплата соответствует роли.** Три таблицы одна под другой.

**Сводка по категориям.** По каждой из пяти оценок: сколько человек, какая доля от всех оцениваемых, у скольких из них зарплата подтверждена, медианная зарплата категории, медианная база сравнения, суммарное отклонение в месяц и в год, и сколько нужно доплатить до нижнего порога.

На текущих данных:

| Оценка | Людей | Доля | Подтв. ЗП | Сумма отклонения, год |
|---|---|---|---|---|
| 1. Недоплата (Compa < 0.80) | 25 | 16% | 13 | −25.2 млн ₽ |
| 2. Ниже роли (0.80–0.90) | 14 | 9% | 10 | −7.3 млн ₽ |
| 3. Соответствует роли (0.90–1.15) | 99 | 64% | 61 | +1.1 млн ₽ |
| 4. Выше роли (1.15–1.30) | 7 | 4% | 5 | +4.9 млн ₽ |
| 5. Переплата (> 1.30) | 11 | 7% | 9 | +16.1 млн ₽ |

Две трети сотрудников получают по своей роли. Из оставшейся трети недоплаченных вдвое больше, чем переплаченных, — но переплата на человека крупнее, поэтому в деньгах разрыв меньше, чем кажется по числу людей.

**Разбивка по проектным ролям.** Та же оценка в разрезе ролей. Отвечает на вопрос, где перекос системный, а не индивидуальный. Например, у руководителей продукта 9 недоплаченных и 5 «ниже роли» из 39 — больше трети роли оплачивается ниже её же медианы, и это уже не про отдельных людей.

**Разбивка по проектам.** Три группы оценки по направлениям, с итогами. Показывает, есть ли направления, где недоплата или переплата концентрируется. Строка «Силовой блок»: 8 недоплаченных из 18 — повод посмотреть на это направление отдельно.

## Лист «Роли»

**База сравнения в явном виде.** Медиана из этого листа — то самое число, с которым сравнивается каждая зарплата. Прежде чем верить оценке конкретного человека, посмотрите сюда.

### Столбцы

> **Имена здесь короче, чем в основной таблице.** На этом листе `Q1`, `медиана`, `Q3`; в листе «Назначения», который грузится в DataLens, те же величины называются `Q1 роли`, `Медиана роли`, `Q3 роли`. Формулы чартов пишутся по вторым именам — лист «Роли» в DataLens не загружается.


Разберём на администраторах проекта — у них видны все эффекты. Их 44 оклада по возрастанию, в тысячах:

```
31  71 71 71 71 71 71  74 74  115 │ 120 ×17  124 125 125 129 │ 130 ×7  142 150 │ 205 230 250 250
    ← 10 человек ниже Q1          ↑Q1        ↑медиана        ↑Q3      → 6 человек выше Q3
```

**`Q1` и `Q3` — границы четвертей.** Отсортированные оклады роли делятся на четыре равные группы. Q1 = 120 000: четверть людей получает меньше. Q3 = 130 000: три четверти получают меньше, верхняя четверть начинается отсюда. Между ними лежит **средняя половина роли** — типичный коридор оплаты. У администраторов он узкий (120–130 тыс), хотя вся роль растянута от 31 до 251 тыс.

Нужны для двух вещей: на чарте разброса рисуются тонкими линиями вокруг медианы, и из них считаются границы выбросов — `Q3 + 1,5 × (Q3 − Q1)` и `Q1 − 1,5 × (Q3 − Q1)`.

У специалистов Q1 = медиана = Q3 = 71 966: три четверти роли на одной ставке. Коробка схлопывается в точку, и правило выбросов для роли отключается.

**`СКО` — стандартное отклонение.** Типичное расстояние от среднего: у администраторов 43 432 ₽ при среднем 123 006 ₽. Само по себе мало что говорит, нужно для двух вещей:

- из него считается `Z-score` = `(ЗП − среднее) / СКО`. Сотрудник с окладом 250 693 даёт +2,94 — почти три типичных расстояния выше среднего;
- отношение СКО к среднему показывает однородность роли: 35% у администраторов, 23% у руководителей продукта, 16% у специалистов. Чем выше, тем осторожнее с выводами по этой роли.

**`разброс, раз` — максимум / минимум.** Грубая, но мгновенно читаемая мера неоднородности:

| Роль | Мин | Макс | Разброс |
|---|---|---|---|
| Специалист | 71 966 | 130 000 | 1,8× |
| **Администратор проекта** | 31 392 | 250 693 | **8,0×** |
| Исполнитель функции | 89 316 | 348 252 | 3,9× |
| Руководитель продукта | 120 000 | 354 383 | 3,0× |
| Руководитель проекта | 139 590 | 623 491 | 4,5× |

Чувствителен к одному человеку: восьмикратный разброс у администраторов почти целиком создаёт один оклад 31 392 ₽ — стажёрская ставка на администраторской роли. Без него разброс падает до 3,5×. Поэтому смотрите пару «разброс + СКО»: первый ловит крайние случаи, второй описывает основную массу.

**`граница выброса вниз` / `граница выброса вверх`** — `Q1 − 1,5 × (Q3 − Q1)` и `Q3 + 1,5 × (Q3 − Q1)`. Всё, что за ними, помечается в основной таблице как выброс. Эти же два числа рисуются «усами» на чарте разброса.

**`правило IQR применимо`** — «нет, плоский тариф» означает, что Q1 = Q3, межквартильный размах нулевой и границы схлопнулись в точку. Для такой роли правило выбросов отключено: иначе выбросом становилось бы отличие хоть на рубль. Сейчас так у трёх ролей — специалист (22 из 24 на одной ставке), аналитик и руководитель функции.

**`используется как база`** — участвует ли медиана роли в сравнении:

- **«да»** — в роли четыре и больше человек, медиана достоверна. Сейчас так у пяти ролей.
- **«нет, откат на уровень»** — людей меньше четырёх. У роли из одного человека медиана равна его же зарплате, и Compa-ratio вышел бы ровно 1,00 — роль сравнивалась бы сама с собой. Такие роли сравниваются с медианой соседних ролей своего уровня.

Сейчас на откате «Аналитик» (1 человек, база 249 980 ₽ по коллегам из R3) и «Руководитель функции» (2 человека, база 350 000 ₽ по руководителям проектов из R4). Порог задан константой `MIN_GROUP_SIZE`.

## Лист «Проекты»

Сводка по направлениям: численность, FTE, число продуктов, ФОТ в месяц, стоимость полной ставки, число вакансий, доля в общем ФОТ. Отсортировано по ФОТ.

**Продукты считаются по всем строкам, включая вакансии.** Продукт существует независимо от того, укомплектован он или нет: в «Окружающем мире» пять продуктов, но «Регистрация блоггеров» держится на одной незакрытой вакансии руководителя проекта. Такие продукты вынесены в отдельную колонку **«продуктов без людей»** — иначе они пропадали бы из счёта, и сумма по проектам не сходилась бы с общим числом продуктов.

Численность, FTE и ФОТ, наоборот, считаются только по людям: вакансия не человек и зарплату не получает.

Зачем нужен: быстрая проверка, что суммы сошлись, и готовый ответ на вопрос «во что обходится каждое направление» без открытия DataLens.

**Лист пересчитывается вручную.** Округление делается до расчёта производных, а не после, поэтому показанные числа делятся друг на друга и дают показанный результат:

```
Окружающий мир:  1 848 508,64 / 9,6666 = 191 226,35 ₽
```

FTE выводится с четырьмя знаками именно поэтому. С одним знаком (9,7) деление дало бы 190 567,90 ₽ — цифру, которой в листе нет, и проверка выглядела бы как ошибка.

`FTE` здесь — сумма столбца `Доля занятости` из листа «Назначения», то есть тоже округлённого. У сотрудника на трёх продуктах доля 0,3333, и три доли дают 0,9999 — отсюда FTE вида 9,6666 вместо 9,6667. Это сделано намеренно: лист должен сходиться с листом «Назначения», а не с идеальной дробью.

### Доли всегда суммируются ровно в 100%

Колонки `доля ФОТ, %` (лист «Проекты») и `доля, %` (лист «Оценка ЗП») округляются **методом наибольших остатков**, а не каждая сама по себе.

Обычное округление каждой доли независимо даёт в сумме 99,9 или 100,1: отброшенные сотые накапливаются. Это не ошибка расчёта, но в таблице выглядит как ошибка, и её приходится каждый раз объяснять. Поэтому доли округляются вниз, а недостающие десятые отдаются тем строкам, у которых отброшенный остаток был самым большим. Сумма получается ровно 100,0 при любых данных.

Заодно убрано двойное округление: раньше доля сначала округлялась до одного знака, а потом вся таблица — до целых, и 63,46% превращалось в 64%. Теперь доли считаются после округления остальных колонок и не участвуют в нём.

## Лист «Вакансии»

35 открытых позиций с оценкой стоимости каждой — медиана по её проектной роли. Отсортировано по стоимости.

Зачем нужен: прогноз прироста ФОТ при закрытии набора. На текущих данных закрытие всех позиций обойдётся примерно в **5.9 млн ₽/мес, 70 млн ₽/год — плюс 18% к текущему ФОТ**. Такой цифры в исходной таблице не видно вообще, а для бюджетирования она важнее большинства остальных.

## Лист «Выравнивание · сводка»

Сколько стоит убрать провалы в оплате — тремя вложенными сценариями. Одна строка на сценарий, **отсюда берутся итоги**.

| Код | Сценарий | Кого включает | Ступени | Людей | ₽/мес | ₽/год | % ФОТ |
|---|---|---|---|---:|---:|---:|---:|
| **A** | обязательный минимум | Compa-ratio ниже 0,80, ЗП подтверждена | 1 | 13 | 741 878 | 8,9 млн | 2,34 |
| **B** | по проверенным данным | все ниже 0,90 с подтверждённой ЗП | 1, 2 | 24 | 920 194 | 11,0 млн | 2,90 |
| **C** | верхняя оценка | плюс неподтверждённые зарплаты | 1, 2, 3 | 41 | 1 852 963 | 22,2 млн | 5,85 |

Сценарии вложены: каждый следующий включает предыдущий. Смысл деления — не в точности оценки, а в том, чтобы разговор с финансами шёл про выбор из трёх с понятной ценой, а не про одну цифру «сколько стоит справедливость».

Обратите внимание на соотношение A и B: **13 человек из 24 дают 80% стоимости**. Самые глубокие провалы — это и есть почти все деньги, и начинать логично с них.

Доплата считается до нижнего порога (0,90 от базы сравнения), а не до самой базы: цель — убрать провалы, а не выровнять всех в одну точку. Суммы gross, без страховых взносов: ставка зависит от юрлица, а его в расчёте нет.

## Лист «Выравнивание · люди»

Поимённый список: кому и сколько доплачивать, с колонками `ЗП после выравнивания` и `Compa-ratio после`. Связан со сводкой ключом **`Код сценария`**.

**Лист развёрнут по сценариям.** Человек из ступени 1 входит и в A, и в B, и в C, поэтому даёт три строки; из ступени 2 — две; из ступени 3 — одну. 41 человек превращается в 78 строк.

Так вложенность сценариев становится частью данных: обычный селектор по колонке `Сценарий` работает накопительно, без параметров датасета и вычисляемых полей в BI. Выбрали B — видите 24 человек, выбрали A — 13.

> ⚠️ **Плата за это — обязательный разрез по сценарию.** Сумма `Доплата до порога` по всему листу без группировки даёт 3 515 035 ₽ вместо 1 852 963: ступень 1 посчитается трижды. Итоги берите со сводки, а на дашборде делайте селектор сценария обязательным полем со значением по умолчанию.

| Столбец | Что означает |
|---|---|
| `Код сценария` | `A` / `B` / `C` — ключ связи со сводкой |
| `Сценарий` | читаемое название, оно же значение селектора |
| `Уровень выравнивания` | 1, 2 или 3 — собственная ступень человека |
| `Сценарий выравнивания` | она же словами: почему человек в этой ступени |
| `Блок`, `Проект` | все места работы человека через `; ` — он может быть занят в нескольких |
| `Доплата до порога` | сколько не хватает до Compa-ratio 0,90 |
| `ЗП после выравнивания` | `ЗП` + доплата |
| `Compa-ratio после` | всегда 0,900 — проверка, что доплата посчитана верно |

Тот же список отдельным файлом — `datalens_dataset_alignment.csv`, для подключения своим датасетом.

Как и на листе «Сотрудники», группировать по `Блок` или `Проект` здесь нельзя: у людей с несколькими местами работы это склеенная строка. Сейчас таких трое из 41 — сотрудники 25, 103 и 132.

## Лист «Изменения»

Движение людей и денег между двумя последними срезами, выстроенное **лесенкой**.

`Было` и `Стало` — это **ФОТ до и после каждого события**, а не зарплата одного человека. Строки идут подряд: «Стало» предыдущей равно «Было» следующей, а «Стало» последней строки — это ФОТ нового среза.

```
Событие          Сотрудник   Было          Стало         Δ руб        Δ %
Ушёл             68          31 692 786    31 069 295    −623 491     −1,97
Ушёл             87          31 069 295    30 692 350    −376 945     −1,21
Пришёл           900         30 692 350    30 892 350    +200 000     +0,65
ЗП изменилась    119         30 892 350    30 951 672     +59 322     +0,19
Роль изменилась  53          30 951 672    30 951 672           0      0,00
```

Так на каждой строке сразу видно не только что произошло, но и **насколько от этого сдвинулся фонд** — в рублях и процентах. Совпадение последнего «Стало» с ФОТ нового среза — встроенная сверка: не сошлось, значит событие потерялось.

| Событие | Когда появляется | Δ руб |
|---|---|---|
| `Ушёл` | человек был в прошлом срезе и пропал | −его зарплата |
| `Пришёл` | появился новый | +его зарплата |
| `ЗП изменилась` | зарплата другая | разница |
| `Роль изменилась` | сменилась проектная роль | 0 |
| `Руководитель изменился` | перешёл под другого руководителя | 0 |
| `Продукты изменились` | изменился набор продуктов | 0 |
| `Изменений нет` | срез совпал с предыдущим | 0 |

Порядок строк не случаен — по нему считается лесенка: сначала уходы, потом приходы, потом пересмотры зарплат, потом перестановки без денег. Внутри каждого типа — от самого крупного.

Столбцы `Было` и `Стало` заняты фондом, поэтому **что именно изменилось у человека, написано в `Комментарии`**: `320 678 ₽ → 380 000 ₽ (+18,5% к своей ЗП)`, `Исполнитель функции → Руководитель функции`, `Руководитель проекта, 623 491 ₽`.

Столбцы `Блок`, `Проект`, `Продукт` перечисляют все места работы человека через `; `. Продукт — коротким названием: полное `Проект / Продукт` дублировало бы соседний столбец.

На первом запуске лист состоит из одной строки «Первый срез»: сравнивать не с чем.

## Лист «Изменения · прочее»

Всё, что к ФОТ отношения не имеет и потому не встаёт в лесенку.

| Событие | Что показывает |
|---|---|
| `Сотрудников` | численность до и после, с процентом |
| `Открытых вакансий` | число позиций до и после |
| `Вакансия открыта` / `Вакансия закрыта` | изменилось число позиций на конкретном продукте |
| `Вакансия висит` | позиция открыта третий срез подряд |
| `⚠ Проверьте нумерацию` | состав сменился больше чем на 20% |

**Последняя строка — про целостность истории.** Сравнение сшивается по столбцу `Сотрудник`. Если обезличивание прогонять заново и раздавать номера в новом порядке, лист покажет, что уволились все и пришли все. Таблицу соответствия «ФИО → номер» нужно хранить отдельно и никогда не перегенерировать с нуля — новым людям выдавать следующие свободные номера.

## Лист «Проверки»

Контроли качества данных. Первые семь строк помечены «справочно» — это просто количества. Остальные должны быть нулями, кроме двух:

- **«ЗП не подтверждена»** — сколько человек имеют комментарий вида «проверить ЗП», но при этом в ячейке стоит число. Это заглушки, и пока их 58 из 180, треть выводов дашборда — про несуществующие зарплаты.
- **«Ролей без достаточной выборки»** — сколько человек сравниваются по откату на уровень роли. Не ошибка, но при разборе их случаев стоит помнить, что выборка мала.

Открывать первым при каждом обновлении данных.

---

# Часть 2. Столбцы листа «Назначения» (основной таблицы)

49 столбцов, сгруппированы по смыслу.

## Откуда и кто (столбцы 1–14)

| Столбец | Тип | Что означает |
|---|---|---|
| `Дата среза` | дата | Месяц, к которому относится выгрузка. Заполняется флагом `--snapshot`. Нужен, чтобы складывать месячные срезы в одну таблицу и видеть динамику |
| `Строка` | целое | Номер строки исходной таблицы. Служит для поиска: нашли проблему в дашборде — по этому номеру находите строку в Excel |
| `Блок` | текст | Столбец «Ответственный за направление». Переименован, потому что это уровень **над** проектом: четверо ответственных на двенадцать проектов |
| `Проект` | текст | Направление. Их двенадцать |
| `Продукт кратко` | текст | Название продукта как единицы учёта. **Именно так продукты считаются везде:** 57 значений. Под одним коротким именем в исходнике лежит несколько систем — «Мобильные приложения» это Робот Макс, МПГУ, Услуги ФНС и ещё три |
| `Продукт` | текст | Полное название конкретной системы. Справочно: по нему видно, на чём именно открыта вакансия, и по нему проверяются дубли назначений |
| `Продукт ключ` | текст | `Проект / Продукт кратко`. **Ключ продукта**, по нему считаются `COUNTD`, bus factor и сводка по проектам. Та же единица, что в дереве структуры |
| `Руководитель` | текст | Непосредственный руководитель. Вакантные руководящие позиции нормализованы к виду `Вакансия: ...` |
| `Сотрудник` | **строка** | Идентификатор человека. У вакансий пустой — иначе все открытые позиции склеились бы в одного «сотрудника» под служебной заглушкой. Тип строковый специально: числовой превратил бы «176» в «176.0» на всех подписях |
| `Организация` | текст | Юрлицо, где человек оформлен. **Справочно.** Нужно для ставок страховых взносов и понимания, кто на кого оформлен. В расчёте зарплат не участвует: медианы по организациям внутри одной роли совпадают, действует единая тарифная сетка |
| `Штатная должность` | текст | Должность по штатному расписанию. **Справочно, в расчётах не участвует** — см. «Главное правило» выше |
| `Проектная роль` | текст | Что человек делает в проекте. **Это и есть база сравнения зарплат.** Восемь значений |
| `Уровень роли №` | целое | Порядковый номер уровня, 0–4. Нужен, чтобы роли на осях шли по старшинству, а не по алфавиту |
| `Уровень роли` | текст | R0 Стажировка → R1 Исполнение → R2 Администрирование → R3 Ведение продукта / функции → R4 Руководство проектом / функцией. Группировка ролей по объёму ответственности. Используется как откат, когда в роли меньше четырёх человек |

## Зарплата (столбцы 15–19)

| Столбец | Тип | Что означает |
|---|---|---|
| `Вакансия` | 0/1 | 1 — открытая позиция, человека нет. **Вакансии не должны попадать ни в численность, ни в ФОТ, ни в среднюю зарплату** |
| `ЗП исходная` | текст | Как было в исходной ячейке, включая слово «Вакансия». Оставлено для сверки |
| `ЗП` | число | Оклад gross. У вакансий пусто |
| `ЗП подтверждена` | 0/1 | 0 — в комментарии написано «ЗП не указана», «не заполнена» или «проверить ЗП», но при этом в ячейке стоит число. Значит это заглушка, а не факт. **Ставьте фильтр `= 1` на всех чартах справедливости** |
| `Комментарий` | текст | Исходный комментарий как есть |

## Занятость (столбцы 20–26)

| Столбец | Тип | Что означает |
|---|---|---|
| `Назначений у сотрудника` | целое | Сколько строк в исходнике на этого человека. Не то же, что продуктов: четыре системы одного продукта дают четыре назначения |
| `Продуктов у сотрудника` | целое | На скольких продуктах занят человек. Считается по `Продукт ключ` |
| `Проектов у сотрудника` | целое | В скольких проектах. Обычно 1: в нескольких проектах заняты 8 человек из 180 |
| `Мультипродуктовый` | Да/Нет | Занят более чем на одном продукте. Таких 23. Считается по продуктам, а не по строкам: девять человек с несколькими назначениями сидят на одном продукте |
| `Мультипроектный` | Да/Нет | Занят более чем в одном проекте |
| `Доля занятости` | 0–1 | `1 / Назначений у сотрудника`. Человек на трёх продуктах даёт по 0.333 на каждый. Округлена до 4 знаков — только для читаемости, на деньги не влияет |
| `Первичное назначение` | 0/1 | Ровно одна строка на человека. **Так считается численность:** `SUM([Первичное назначение])`. Обычный `COUNT` посчитал бы человека столько раз, на скольких продуктах он занят |
| `ФОТ аллоцированный` | число | Доля зарплаты, приходящаяся на эту строку. **Так считаются деньги в любом разрезе:** `SUM([ФОТ аллоцированный])`. Наивная сумма по столбцу `ЗП` завысила бы ФОТ на 51%: 47.9 млн вместо 31.7. **Не повторяйте формулу руками через `SUM([ЗП] / [Назначений у сотрудника])`** — она даёт на 5 000 ₽ меньше: делит в рублях с плавающей точкой и берёт зарплату из каждой строки, а у сотрудника 40 в двух строках указаны разные суммы (130 000 и 120 000). Готовая колонка считает в целых копейках и берёт зарплату из первичного назначения, поэтому сходится с листом «Сотрудники» до копейки |

## База сравнения (столбцы 27–36)

| Столбец | Тип | Что означает |
|---|---|---|
| `В анализе справедливости` | 0/1 | 1 — человек участвует в светофоре. Исключены: вакансии, повторные строки одного человека и роль «Стажёр» (все 24 стажёра получают одну и ту же ставку, сравнивать не с чем — они дали бы 24 идеальные «нормы» и сместили картину). Итого 156 человек из 180 |
| `Людей в роли` | целое | Размер группы сравнения |
| `Медиана роли` | число | Медианная зарплата по проектной роли |
| `Среднее роли` | число | Среднее по роли. Нужно только для Z-score |
| `СКО роли` | число | Стандартное отклонение по роли. Тоже для Z-score |
| `Q1 роли`, `Q3 роли` | число | Первый и третий квартили. Рисуются тонкими линиями на чарте разброса и используются для границ выбросов |
| `Медиана уровня роли` | число | Запасная база: медиана по **соседним ролям** того же уровня, без собственной роли человека. Заполнена только там, где откат реально применился (3 строки из 156) — в остальных пустая, чтобы не отвлекать числом, которое никуда не идёт |
| `База сравнения` | число | **С этим числом сравнивается зарплата.** Медиана роли, а если в роли меньше четырёх человек — медиана уровня |
| `Уровень сравнения` | текст | На чём основан вывод: «Проектная роль» (153 человека, вывод крепкий) или «Уровень роли (мало данных)» (3 человека, выборка мала). **Эту колонку обязательно показывать в реестре** — без неё дашборд создаёт ложную уверенность |

## Отклонение (столбцы 37–46)

| Столбец | Тип | Что означает |
|---|---|---|
| `Compa-ratio` | число | `ЗП / База сравнения`. Главная метрика. 1.00 — ровно по роли, 0.85 — на 15% ниже тех, кто делает ту же работу, 2.09 — вдвое выше |
| `Отклонение руб` | число | `ЗП − База сравнения`. То же самое в деньгах. **Сортировать списки нужно по этому столбцу:** «−22%» и «−130 тыс ₽/мес» ощущаются по-разному, а решение принимают по рублям |
| `Z-score` | число | Насколько отклонение необычно статистически. \|Z\| > 2 — сильный выброс. Compa-ratio отвечает «насколько», Z-score — «насколько это редкость» |
| `Граница выброса вверх` / `вниз` | число | `Q3 + 1.5×IQR` и `Q1 − 1.5×IQR`. Классическое правило выброса |
| `Выброс по IQR` | текст | «Выброс вверх» / «Выброс вниз» / «В норме» / «Неприменимо (плоский тариф)». **Независимая от Compa-ratio проверка**, учитывающая форму распределения роли. Где два метода сходятся — случай самый надёжный: 11 человек помечены и как переплата, и как выброс вверх; 14 — и как недоплата, и как выброс вниз. Подробно — часть 2б |
| `Оценка ЗП` | текст | **Оценка зарплаты относительно медианы её роли.** Пять уровней: «1. Недоплата» (Compa < 0.80) → «2. Ниже роли» (0.80–0.90) → «3. Соответствует роли» (0.90–1.15) → «4. Выше роли» (1.15–1.30) → «5. Переплата» (> 1.30). Цифровой префикс нужен, чтобы легенда сортировалась от недоплаты к переплате, а не по алфавиту |
| `Группа оценки` | текст | Три значения: «Недоплата» / «Соответствует роли» / «Переплата». **Красить чарты нужно по этому столбцу, а не по `Оценка ЗП`:** пять цветовых классов человек надёжно не различает. Цвет несёт направление, текст — степень |
| `Требует внимания` | 0/1 | Compa-ratio вне 0.90–1.30 или \|Z-score\| > 2. Сейчас 50 человек, из них 32 с подтверждённой зарплатой |
| `Доплата до порога` | число | Сколько не хватает человеку до Compa-ratio 0,90. Лежит только на первичном назначении, поэтому `SUM` не задваивает: 22,2 млн ₽/год всего, 11,0 млн по подтверждённым зарплатам |
| `Сценарий выравнивания` | текст | В какой из трёх сценариев попадает человек: `1. Недоплата, ЗП подтверждена`, `2. Ниже роли, ЗП подтверждена`, `3. ЗП не подтверждена`, `0. Не требуется`. Сценарии вложены — подробности на листе «Выравнивание» |

## Вакансии и риски (столбцы 47–49)

| Столбец | Тип | Что означает |
|---|---|---|
| `Оценка стоимости вакансии` | число | Медиана по проектной роли вакансии. Сумма = прогноз прироста ФОТ при закрытии набора: 5.9 млн ₽/мес, +18% |
| `Людей на продукте` | целое | Сколько человек занято на продукте |
| `Bus factor` | текст | «Критично: 1 человек» / «Риск: 2 человека» / «ОК» / «Только вакансии». Из 57 продуктов 7 держатся на одном человеке |

---

# Часть 2б. Как считаются ключевые столбцы — с примерами

## Доля занятости и первичное назначение

Исходная таблица устроена так, что **одна строка = одно назначение**, а не один человек. Сотрудник, занятый на четырёх продуктах, занимает четыре строки с одинаковой зарплатой. Отсюда два столбца, которые не дают этой особенности испортить все суммы.

**`Доля занятости` = 1 / `Назначений у сотрудника`.** Занятость делится поровну между продуктами человека. Пример из данных — сотрудник 176 на четырёх продуктах «Силового блока» с окладом 350 000 ₽:

| Строка | Продукт | ЗП | Назначений | Доля занятости | ФОТ аллоцированный |
|---|---|---|---|---|---|
| 59 | Безопасная среда | 350 000 | 4 | 0.25 | 87 500 |
| 63 | … | 350 000 | 4 | 0.25 | 87 500 |
| 67 | … | 350 000 | 4 | 0.25 | 87 500 |
| 69 | … | 350 000 | 4 | 0.25 | 87 500 |

Сумма долей = 1.00, сумма `ФОТ аллоцированный` = 350 000 ₽ — ровно его оклад. Именно поэтому деньги считаются по `ФОТ аллоцированный`, а не по `ЗП`: суммирование `ЗП` дало бы 1 400 000 ₽ на одного человека. По всей таблице такая ошибка завышает ФОТ на 51%.

Долю можно уточнить вручную, если известно, что человек отдаёт одному продукту 70%, а другому 30%. Равное деление — приближение, но на ваших данных оно почти точное: у 148 человек из 180 всего одно назначение.

**`Первичное назначение` = 1 у первой строки каждого сотрудника, 0 у остальных.** Нужен, чтобы считать людей, а не строки. `SUM([Первичное назначение])` = 180 — столько уникальных сотрудников. `COUNT` по строкам дал бы 232.

У вакансий флаг всегда 0: у них нет идентификатора сотрудника, и человеком они не являются.

> **Важная оговорка.** «Первой» может оказаться строка из любого проекта. Поэтому под фильтром по конкретному проекту `SUM([Первичное назначение])` занизит численность — человек, чья первичная строка относится к другому проекту, не посчитается. В таких разрезах используйте `COUNTD([Сотрудник])`.

### Сверка итога: почему аллоцированный ФОТ и сумма окладов могут не сойтись

Два способа посчитать ФОТ компании должны давать одно число:

```
SUM([ФОТ аллоцированный])                                   = 31 687 786,20
SUM(IF [Первичное назначение]=1 THEN [ЗП] ELSE 0 END)       = 31 692 786,29
```

Расхождение — 5 000,09 ₽, и оно целиком объясняется **одним сотрудником с двумя разными окладами в разных строках** (130 000 в одном проекте и 120 000 в другом). Аллоцированный расчёт берёт каждую строку как есть и даёт 125 000 ₽, второй способ берёт первое встретившееся значение — 130 000 ₽.

Это не погрешность метода, а ошибка в источнике: она отражена в листе «Проверки» строкой «Разные ЗП у одного сотрудника». Пока в исходной таблице не выбрано правильное значение, любой способ подсчёта будет давать свою цифру.

**Если расхождение больше и не сводится к таким случаям** — ищите строки, где сумма долей сотрудника не равна единице, или дубли назначений. Обе проверки есть на том же листе.

## Анализ справедливости: кто в него попадает

`В анализе справедливости` = 1 означает, что зарплата этого человека сравнивается с медианой его роли. Условий четыре, и все должны выполняться одновременно:

1. `Вакансия` = 0 — у открытой позиции нет зарплаты;
2. `Первичное назначение` = 1 — человек участвует один раз, а не столько, на скольких продуктах занят;
3. зарплата заполнена;
4. роль не входит в список ролей с плоским тарифом (сейчас это только «Стажёр»).

Арифметика на ваших данных:

```
267  строк всего
−35  вакансий
−52  повторных строк мультипродуктовых сотрудников
−24  человека на роли «Стажёр»
─────
156  человек в анализе
```

**Почему исключены стажёры.** Все 24 получают ровно одну ставку — 31 392,17 ₽. Медиана по такой группе равна каждому значению, Compa-ratio у всех выходит ровно 1.00. Оставь их в анализе — получим 24 идеальные «нормы», которые ничего не значат, но раздувают долю справедливо оплачиваемых. Если появится ещё одна роль с плоским тарифом, её добавляют в `FLAT_RATE_ROLES` в скрипте.

## Статистика по роли: медиана, среднее, СКО, квартили

Все пять столбцов считаются **по одной и той же группе** — по всем людям с этой проектной ролью, попавшим в анализ справедливости. У всех строк одной роли значения одинаковые: это характеристика группы, а не человека.

| Столбец | Что это | Зачем нужен |
|---|---|---|
| `Медиана роли` | Значение посередине: половина роли получает меньше, половина больше | **База сравнения.** Медиана, а не среднее, потому что она устойчива к выбросам: один человек с окладом 623 тыс сдвинет среднее, но не медиану |
| `Среднее роли` | Среднее арифметическое | Нужно только для расчёта Z-score |
| `СКО роли` | Стандартное отклонение — типичный разброс вокруг среднего | Тоже только для Z-score |
| `Q1 роли` | Первый квартиль: 25% роли получают меньше | Нижняя граница «коробки» на чарте разброса, вход в формулу IQR |
| `Q3 роли` | Третий квартиль: 75% роли получают меньше | Верхняя граница коробки, вход в формулу IQR |

Фактические значения по вашим данным:

| Роль | Людей | Q1 | Медиана | Q3 | Среднее | СКО |
|---|---|---|---|---|---|---|
| Специалист | 24 | 71 966 | 71 966 | 71 966 | 74 764 | 11 810 |
| Администратор проекта | 44 | 120 000 | 120 076 | 130 000 | 123 006 | 43 432 |
| Исполнитель функции | 11 | 150 069 | 180 096 | 210 836 | 188 569 | 73 402 |
| Аналитик | 1 | 220 000 | 220 000 | 220 000 | 220 000 | — |
| Руководитель продукта | 39 | 200 346 | 250 000 | 250 000 | 230 336 | 52 040 |
| Руководитель проекта | 35 | 315 839 | 350 000 | 350 000 | 342 098 | 72 088 |
| Руководитель функции | 2 | 241 000 | 241 000 | 241 000 | 241 000 | 0 |

Обратите внимание на администраторов проекта: медиана 120 076 при среднем 123 006 и СКО 43 432. Большое СКО при близких медиане и среднем означает, что внутри роли есть и очень низкие, и очень высокие оклады — от 31 до 251 тыс. Это самая неоднородная роль в компании.

## База сравнения и уровень сравнения

**`База сравнения`** — то самое число, с которым делится зарплата. Логика в две ступени:

```
если Людей в роли >= 4  →  База сравнения = Медиана роли
иначе                    →  База сравнения = Медиана уровня роли
```

**`Медиана уровня роли`** — запасная база: медиана по ролям того же уровня R0–R4, **исключая собственную роль человека**. Нужна, когда роль слишком мала для собственной статистики: медиана по одному человеку равна его же зарплате, и Compa-ratio у него получился бы ровно 1.00.

Собственная роль исключается, чтобы избежать остаточной цикличности — иначе человек частично сравнивался бы сам с собой. Для аналитика (1 человек в роли) база берётся по 50 коллегам того же уровня R3 — исполнителям функции и руководителям продукта: 249 980 ₽.

Столбец заполнен только у тех строк, где откат реально применился. В остальных он пуст: раньше там стояла «медиана соседей», которая никуда не шла и вызывала закономерный вопрос, откуда взялось число.

> **Почему в выводе скрипта нет числа «медиана уровня».** Оно нигде не участвует в расчётах: база отката берётся по соседним ролям, а не по уровню целиком. При этом оно оказывается обманчиво близким к медиане роли — для R2 это 120 083,24 ₽ против 120 076,12 ₽ у администраторов. Разница в 7 ₽ возникает из-за одного добавленного человека: 44 значения дают медиану как среднее 22-го и 23-го, 45 значений — как само 23-е. Чтобы два похожих числа не путались, скрипт печатает только медианы ролей — те, с которыми зарплаты и сравниваются.

**`Уровень сравнения`** сообщает, какая ступень сработала. Три значения:

| Значение | Строк | Что означает |
|---|---|---|
| **Проектная роль** | 153 | Сравнение с медианой собственной роли. Вывод крепкий |
| **Уровень роли (мало данных)** | 3 | В роли меньше четырёх человек, сравнили с медианой уровня. Вывод слабее — учитывайте при разборе |
| **Не сравнивается** | 111 | Строка вообще не участвует в анализе |

**Почему «не сравнивается» встречается в 111 строках.** Это те же исключения, что выше: 35 вакансий, 52 повторные строки мультипродуктовых сотрудников и 24 стажёра. Строка не считается ошибочной — просто её зарплату не с чем или незачем сравнивать. У таких строк `Compa-ratio`, `Отклонение руб` и `Z-score` пустые, а `Оценка ЗП` = «0. Не оценивается».

Все три человека на откате — реальный случай из ваших данных:

| Сотрудник | Роль | Людей в роли | ЗП | Медиана роли | Медиана уровня | База | Compa-ratio |
|---|---|---|---|---|---|---|---|
| 20 | Руководитель функции | 2 | 241 000 | 241 000 | 350 000 | **350 000** | 0.69 |
| 172 | Руководитель функции | 2 | 241 000 | 241 000 | 350 000 | **350 000** | 0.69 |
| 161 | Аналитик | 1 | 220 000 | 220 000 | 249 980 | **249 980** | 0.88 |

Без отката у всех троих Compa-ratio был бы ровно 1.00 — то есть оба руководителя функции считались бы «в норме», хотя получают на треть меньше остальных на своём уровне ответственности.

## Compa-ratio, отклонение, Z-score

Три метрики отвечают на разные вопросы об одном и том же отклонении.

```
Compa-ratio    = ЗП / База сравнения
Отклонение руб = ЗП − База сравнения
Z-score        = (ЗП − Среднее роли) / СКО роли
```

- **Compa-ratio** — во сколько раз. 1.00 — ровно по роли, 0.85 — на 15% ниже тех, кто делает ту же работу, 2.09 — вдвое выше. Отраслевой стандарт C&B, читается без объяснений.
- **Отклонение руб** — то же самое в деньгах. **Сортировать списки нужно по нему:** «−40%» и «−210 тыс ₽/мес» ощущаются по-разному, а решение принимают по рублям.
- **Z-score** — насколько отклонение необычно для этой роли. Compa-ratio отвечает «насколько», Z-score — «насколько это редкость». В роли с широким разбросом отклонение на 30% может быть обычным делом, в узкой — исключением. Порог |Z| > 2 означает, что человек за пределами того, что встречается у 95% его коллег.

Три примера из ваших данных:

**Сотрудник 120, администратор проекта, 250 693 ₽**
```
Compa-ratio = 250 693 / 120 076 = 2.09
Отклонение  = 250 693 − 120 076 = +130 617 ₽
Z-score     = (250 693 − 123 006) / 43 432 = +2.94
Оценка: 5. Переплата
```
Получает вдвое больше медианы своей роли. Z-score почти 3 — таких в роли единицы. В графе штатной должности у него «Руководитель проекта»: должность объясняет оклад, но работу он делает администраторскую. На расчёт должность не влияла.

**Сотрудник 58, руководитель проекта, 139 590 ₽**
```
Compa-ratio = 139 590 / 350 000 = 0.40
Отклонение  = 139 590 − 350 000 = −210 410 ₽
Z-score     = (139 590 − 342 098) / 72 088 = −2.81
Оценка: 1. Недоплата
```
Самое крупное отклонение вниз в компании: 210 тыс ₽ в месяц ниже медианы своей роли.

**Сотрудник 133, руководитель продукта, 156 940 ₽**
```
Compa-ratio = 156 940 / 250 000 = 0.63
Отклонение  = 156 940 − 250 000 = −93 060 ₽
Z-score     = (156 940 − 230 336) / 52 040 = −1.41
Оценка: 1. Недоплата
```
Здесь Compa-ratio и Z-score расходятся: по отношению к медиане это сильная недоплата, но для роли с широким разбросом такое значение не редкость (|Z| < 2). Полезный случай: метрики дополняют друг друга, а не дублируют.

## Границы выброса и правило IQR

Классический статистический тест на выброс, **независимый от Compa-ratio**.

```
IQR                   = Q3 роли − Q1 роли        (межквартильный размах)
Граница выброса вверх = Q3 роли + 1.5 × IQR
Граница выброса вниз  = Q1 роли − 1.5 × IQR
```

Всё, что за границами, помечается в `Выброс по IQR` как «Выброс вверх» или «Выброс вниз». Множитель 1.5 — стандартный: именно так строятся «усы» у диаграммы размаха.

Ключевое отличие от Compa-ratio: **IQR учитывает форму распределения роли**. Порог 0.90 одинаков для всех, а границы IQR у каждой роли свои. У руководителей проекта коридор 264 597 – 401 242 ₽ (узкий, роль однородна), у исполнителей функции 58 918 – 301 988 ₽ (широкий, роль разнородна). Один и тот же оклад 200 тыс будет выбросом в первой роли и нормой во второй.

**Значение «Неприменимо (плоский тариф)».** Если у роли Q1 = Q3, межквартильный размах равен нулю, границы схлопываются в одну точку, и «выбросом» становится отличие хоть на рубль. Так у специалистов: 22 человека из 24 получают ровно 71 966,24 ₽. Правило для такой роли бессмысленно, поэтому оно отключается — Compa-ratio при этом продолжает работать.

**Зачем нужны два метода.** Совпадение двух независимых проверок — самый надёжный сигнал. На ваших данных:

| | В норме | Выброс вверх | Выброс вниз | Неприменимо | Всего |
|---|---|---|---|---|---|
| Недоплата | 23 | 0 | **14** | 2 | 39 |
| Соответствует роли | 76 | 0 | 0 | 23 | 99 |
| Переплата | 5 | **11** | 0 | 2 | 18 |

25 человек подтверждены обоими методами — с них разумно начинать разбор. Ни один человек из категории «соответствует роли» не оказался статистическим выбросом: методы не противоречат друг другу, они различаются только строгостью.

---

# Часть 3. Шпаргалка: как посчитать правильно

Самые частые ошибки на этом датасете и как их избежать.

| Что считаем | ❌ Неправильно | ✅ Правильно | Почему |
|---|---|---|---|
| Численность | `COUNT([Сотрудник])` | `SUM([Первичное назначение])` | Иначе человек считается столько раз, на скольких продуктах занят |
| Численность в срезе по проекту | `SUM([Первичное назначение])` | `COUNTD([Сотрудник])` | «Первичной» может оказаться строка из другого проекта |
| ФОТ | `SUM([ЗП])` | `SUM([ФОТ аллоцированный])` | Наивная сумма завышает на 51% |
| Средняя зарплата | `AVG([ЗП])` | `SUM(IF [Первичное назначение]=1 THEN [ЗП] ELSE 0 END) / SUM([Первичное назначение])` | Иначе мультипродуктовые считаются несколько раз |
| Любая метрика по людям | без фильтра | `[Вакансия] = 0` | 35 вакансий исказят численность и среднюю зарплату |
| Справедливость оплаты | без фильтра | `[В анализе справедливости] = 1` | Вакансии, повторные строки и стажёры не сравниваются |
| Отклонения | без фильтра | `[ЗП подтверждена] = 1` | Иначе 18 из 50 находок будут про зарплаты-заглушки |
| Цвет на чартах | по `Оценка ЗП` | по `Группа оценки` | Пять цветовых классов неразличимы |
| Продукты | `COUNTD([Продукт])` | `COUNTD([Продукт ключ])` | Полных названий 71, но это системы внутри продуктов; продуктов 57 |

---

# Часть 4. Что можно менять в скрипте

Три места, где настройки вынесены наверх и меняются без риска.

**`ROLE_LEVELS`** — распределение восьми проектных ролей по уровням R0–R4. Если появится новая роль, добавьте её сюда, иначе она попадёт в «Не классифицировано» и выпадет из анализа (проверка «Ролей вне классификатора» это покажет).

**`MIN_GROUP_SIZE = 4`** — минимальный размер роли, при котором медиана по ней считается достоверной. Повысите — больше людей будет сравниваться по уровню, вывод станет грубее, но устойчивее.

**`LOWER_THRESHOLD = 0.90` и `UPPER_THRESHOLD = 1.30`** — границы светофора. Сузите — список на ревью вырастет.

После любого изменения скрипт заново печатает проверку монотонности медиан по уровням ролей. **Если увидели «НАРУШЕНА» — классификация ролей неверна, и откат на уровень будет искажать базу сравнения.**
