// ============================================================================
//  Вкладка Params — значения параметров по умолчанию (массивы строк).
// ============================================================================

module.exports = {
    // Режим стека: 'abs' — абсолютные значения, 'percent' — доли.
    stacking: ['abs'],

    // Подписи-итоги в конце полосы: 'on' | 'off'.
    totals: ['on'],

    // Фильтр по годам. Пустая строка = без фильтра.
    year: [''],
};
