/**
 * DataLens · Advanced-чарт · вкладка Meta
 *
 * Перечень связанных сущностей. Ключ — произвольный алиас, значение —
 * id датасета. Алиас потом достаётся на вкладке Sources через
 * Editor.getId('hrTree'). Описывать id обязательно: по нему DataLens
 * понимает, с какими объектами связан чарт, и это нужно для диалога
 * связанных объектов, копирования воркбука и публикации.
 *
 * Где взять id: открыть датасет «HR · Иерархия» → меню → «Копировать ID».
 * Он же виден в адресной строке: …/datasets/<вот_это>/…
 *
 * Внимание: на этой вкладке НЕ пишут module.exports — только объект.
 */

{
    "links": {
        "hrTree": "ЗАМЕНИТЕ_НА_ID_ДАТАСЕТА"
    }
}
