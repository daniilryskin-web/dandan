/**
 * DataLens · Advanced-чарт · вкладка Sources
 *
 * Запрос к датасету «HR · Иерархия». Алиас hrTree объявлен на вкладке Meta.
 *
 * ПОРЯДОК КОЛОНОК ВАЖЕН: он должен совпадать с массивом COLUMNS на вкладке
 * Prepare. Имена колонок чарт берёт из ответа (структура Type), а COLUMNS —
 * подстраховка на случай, если формат ответа окажется другим.
 *
 * Если buildSource ругается на ключ datasetId, замените его на id:
 * в разных версиях документации встречаются оба написания.
 */

const {buildSource} = require('libs/dataset/v2');

module.exports = {
    tree: buildSource({
        datasetId: Editor.getId('hrTree'),
        columns: [
            'Блок',
            'Проект',
            'Руководитель',
            'Продукт кратко',
            'риск',
            'R4',
            'R3',
            'R2',
            'R1',
            'R0',
            'людей',
            'вакансий',
            'ФОТ_мес'
        ]
    })
};
