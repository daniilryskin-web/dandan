// ============================================================================
//  Вкладка Params — значения параметров по умолчанию (массивы строк).
// ============================================================================

module.exports = {
    // Значение поля «Тип», по которому берётся показатель.
    // Пустая строка = взять список FILTER_VALUES из вкладки Prepare.
    filter: [''],
};
