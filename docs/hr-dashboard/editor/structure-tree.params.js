/**
 * DataLens · Advanced-чарт · вкладка Params
 *
 * Параметры чарта по умолчанию. Значения — массивы строк, даже когда
 * значение одно. Всё, что приходит из селекторов дашборда или из URL,
 * должно быть объявлено здесь, иначе не подставится.
 *
 * Дерево строится по всем данным и фильтруется селекторами дашборда
 * на уровне датасета, поэтому собственных параметров ему не нужно.
 * Пустой объект — рабочее значение, вкладку просто нельзя оставить
 * незаполненной.
 */

module.exports = {};
