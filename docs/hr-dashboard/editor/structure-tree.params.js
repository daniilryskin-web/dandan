/**
 * DataLens · Advanced-чарт · вкладка Params
 *
 * Параметры чарта по умолчанию. Значения — всегда МАССИВЫ СТРОК, даже когда
 * значение одно. Всё, что приходит из селекторов дашборда или из URL,
 * должно быть объявлено здесь: незаявленный параметр просто не дойдёт.
 *
 * Четыре параметра — это четыре селектора дашборда. Пустой массив означает
 * «все», то есть без фильтра. Фильтрация выполняется на вкладке Prepare,
 * до построения дерева.
 *
 *   block    → колонка «Блок»           (ответственный за направление)
 *   project  → колонка «Проект»         (направление)
 *   chief    → колонка «Руководитель»   (руководитель продукта)
 *   product  → колонка «Продукт кратко» (продукт)
 *
 * Именно эти имена нужно указать в связях селекторов на дашборде —
 * «по параметру», а не «по полю». Подробности в 10-structure-tree-howto.md.
 */

module.exports = {
    // Латинские имена — то, что задаётся в селекторе типа «Ручной ввод»
    // в поле «Поле или параметр».
    block: [],
    project: [],
    chief: [],
    product: [],
    // Пятый селектор — выпадающий список сотрудников. Значения берутся
    // из колонки «Сотрудник» датасета, поэтому список обновляется сам.
    employee: [],

    // Поиск сотрудника по ФИО. Значение приходит из поля ввода, объявленного
    // на вкладке Controls, — оно рисуется в шапке самого чарта. Пустая
    // строка означает «не ищем»: дерево показывается целиком.
    // Тот же параметр можно завести селектором дашборда типа «Ручной ввод»,
    // если поиск удобнее держать рядом с остальными селекторами.
    search: [''],

    // Те же фильтры под названиями полей датасета. Селектор на основе
    // датасета в части версий передаёт значение под именем поля, а не под
    // латинским псевдонимом. Объявлены оба варианта, чтобы селектор
    // заработал при любой настройке; чарт принимает и те, и другие.
    'Блок': [],
    'Проект': [],
    'Руководитель': [],
    'Продукт кратко': [],
    'Сотрудник': []
};
