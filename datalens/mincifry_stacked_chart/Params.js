// ============================================================================
//  Вкладка Params — значения параметров по умолчанию.
//  Значения задаются массивами строк: именно в таком виде их отдаёт
//  Editor.getParams().
// ============================================================================

module.exports = {
    // Режим стека: 'abs' — абсолютные значения, 'percent' — доли.
    stacking: ['abs'],

    // Фильтр по годам. Пустая строка = без фильтра.
    year: [''],
};
