# WB+Ozon Checker v27.2

Программа для сбора карточек Wildberries и Ozon, извлечения ссылок на документы,
сверки с реестрами (ФСА, SWIS, BelGISS, EAEU) и выдачи финального вердикта.

## Что нового в v27.2

- Ozon: исправлено двойное URL-кодирование поискового запроса, из-за которого кириллический поиск мог возвращать 0 карточек.
- Ozon: добавлен рекурсивный разбор новых search-виджетов и браузерный fallback поиска через публичную страницу Ozon.
- Ozon: ссылки на реестры теперь извлекаются из всех `widgetStates`, а не только из `webProductDocuments`.
- Ozon: этап реестров включён в основной `run()`, ФСА обогащается только браузерным путём через Playwright.
- Добавлены unit-тесты для новых сценариев Ozon.

## Что нового в v27.1 (хотфикс)

- `install_windows.bat` и `run_windows.bat` переписаны в ASCII (без BOM, с CRLF) —
  больше не ломаются на Windows cmd
- На главном экране — баннер о недостающих Python-пакетах
- Кнопка «Диагностика» в правом верхнем углу показывает версию Python, статус
  движков, недостающие пакеты и последнюю команду subprocess
- При ошибке прогона UI автоматически переключается на вкладку «Логи»

## Установка на Windows

1. Скачайте Python 3.10 или новее с https://python.org. **Обязательно поставьте
   галочку «Add Python to PATH»** при установке.
2. Распакуйте архив `WBChecker_v27_final.zip` в любую папку.
3. Двойной клик по `install_windows.bat`. Дождитесь сообщения «Installation finished».
4. Двойной клик по `run_windows.bat`. Откроется окно приложения.

Если двойной клик не запускает приложение — откройте `cmd` в папке с программой
и выполните:
```
python -m pip install -U -r requirements.txt
python -m playwright install chromium
python wb_checker.py
```

## Структура файлов

| Файл | Назначение |
|---|---|
| `wb_checker.py` | Главное pywebview-приложение с UI |
| `main_v39.py` | WB query-движок (поиск по запросу + реестры) |
| `main_brand.py` | WB brand-движок (поиск по бренду) |
| `ozon_parser.py` | Ozon-парсер (composer-api + browser fallback + карточки + реестры; ФСА только браузером) |
| `fsa_enhanced.py` | Расширенный парсер реестра ФСА (54 поля) |
| `wb_enhanced.py` | Устойчивый сбор продавца WB и плашки «Оригинал» |
| `wb_gui_modern.py` | Альтернативный UI (customtkinter) |
| `requirements.txt` | Список Python-зависимостей |
| `install_windows.bat` | Установщик зависимостей для Windows |
| `run_windows.bat` | Запуск приложения |

## Режимы работы

1. **WB · Поиск (полный)** — два этапа: сбор карточек + проверка реестров
2. **WB · Поиск (этап 1)** — только сбор ссылок (быстро)
3. **WB · Поиск (этап 2)** — проверка реестров по сохранённому CSV
4. **WB · По бренду** — поиск всех карточек указанного бренда
5. **Ozon · Поиск** — поиск + парсинг реестров для Ozon
6. **Unified · WB + Ozon** — параллельный прогон обоих маркетплейсов с единым
   Excel-отчётом

## Реестры

- **ФСА** (`pub.fsa.gov.ru`) — браузерный SPA-путь через Chromium/Playwright; прямой HTTP/API для production-обогащения Ozon отключён
- **SWIS** (`swis.trade.kg`) — полный парсинг HTML
- **BelGISS** и **EAEU** — собирается только ссылка на документ

## Если что-то не работает

1. Откройте приложение и нажмите кнопку «Диагностика» — она покажет, чего не хватает.
2. Если в логах появляется `ModuleNotFoundError` — снова запустите `install_windows.bat`.
3. Если карточки не парсятся, но программа запускается — проверьте, что
   `playwright install chromium` прошёл успешно.

## Лицензия

Внутренний инструмент. Все права на товарные знаки принадлежат их владельцам.
